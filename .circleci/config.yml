version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:10
    steps:
      - run: sudo npm i
      - run: sudo npm install -g @ionic/cli cordova-res
      - run: sudo npm install -g firebase-tools
      - run: ionic build --prod
      - run: firebase deploy
